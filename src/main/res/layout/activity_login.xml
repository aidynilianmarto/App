<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.pc.olx.LoginActivity">

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:id="@+id/imageView"
        android:src="@drawable/olx"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="@dimen/default_button_size_length"
        android:layout_height="wrap_content"
        android:id="@+id/fb_login_button"
        android:background="@drawable/fblogin"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="@dimen/default_button_size_length"
        android:layout_height="wrap_content"
        android:text="Login with mail"
        android:background="@color/ColorWhite"
        android:textColor="@color/ColorBlack"
        android:id="@+id/login_with_mail_button"
        android:layout_below="@+id/fb_login_button"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
